node_modules
.next
out
dist
.env
.env.local
.env.test
.env.*.local
# Google OAuth client secrets — must never land in the repo. Real secret
# was moved to ~/Secrets/google-oauth/ on 2026-05-12.
client_secret_*.json
# Scratch brainstorm/spec docs — kept locally as historical context but
# not part of the codebase.
AI-generated-prompt_*.md
.DS_Store
*.log
.vercel
.turbo
coverage
.supabase
.cache
*.tsbuildinfo
next-env.d.ts
# Supabase CLI local state — pooler URL, project ref, version pins. Auto-
# regenerated by `supabase link` / `db push`; keep out of source control.
supabase/.temp/
# Claude Code per-machine permission settings. Touched on every tool run,
# so leaving it tracked produces noisy diffs. Team-shared .claude/ files
# (commands/, agents/, project CLAUDE.md) can still be committed.
.claude/settings.local.json
.env.prod

# Generated ARM template (build artefact from Bicep)
infra/main.json
